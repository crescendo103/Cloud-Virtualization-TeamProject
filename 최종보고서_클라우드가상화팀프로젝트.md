# 클라우드 가상화 팀 프로젝트 최종보고서

**과목:** 클라우드가상화기술 1분반
**프로젝트명:** AI 모델 학습을 위한 동적 GPU 자원 할당 시스템
**팀 구성원:** 김동주 · 박준영 · 김승겸 · 정유찬
**소속:** 단국대학교
**제출일:** 2026년 6월 15일

---

## 1. 프로젝트 개요

### 1.1 개발 배경 및 목적

딥러닝 모델을 학습하려면 사용자가 직접 Python, PyTorch, CUDA 및 GPU 실행 환경을 구성해야 한다. 또한 여러 사용자가 하나의 GPU 서버를 공유할 경우 작업 순서와 GPU 상태를 관리하는 별도의 시스템이 필요하다.

본 프로젝트는 Google Cloud Platform(GCP), Amazon Web Services(AWS), Microsoft Azure 등 상용 클라우드가 제공하는 GPU 기반 AI 학습 서비스의 핵심 처리 흐름을 소규모로 직접 구현하는 것을 목표로 한다.

구체적으로, 사용자가 웹 화면에서 이미지 데이터셋(ZIP)과 학습 옵션만 제출하면 서버가 요청을 작업 큐에 등록하고, GPU 가용 상태를 확인하여 적절한 장치를 선택한 뒤 CNN 또는 Linear 모델을 학습한다. 사용자는 대시보드를 통해 작업 상태, GPU 사용률, 학습 진행 상황 및 결과를 실시간으로 확인할 수 있다.

### 1.2 주요 목표

1. 웹 기반 데이터셋 업로드 및 학습 옵션 입력 UI 제공
2. FastAPI를 이용한 학습 요청 접수와 상태 조회 REST API 구현
3. FIFO 방식의 학습 작업 큐 관리
4. `nvidia-smi`를 이용한 GPU 상태 확인 및 가용 장치 자동 선택
5. 로컬(subprocess) 또는 Docker 컨테이너 환경에서 PyTorch 학습 실행
6. 학습 가중치와 loss, accuracy 등 지표 파일 저장
7. 대시보드를 통한 작업·자원 상태 모니터링 및 요청량 예측 표시

### 1.3 팀 역할 분담

| 구분 | 담당자 | 주요 역할 |
| --- | --- | --- |
| 1. Input (프론트엔드) | 김동주 | 데이터셋 업로드 UI, 모델·하이퍼파라미터 입력, FastAPI 요청 전송 |
| 2. Scheduler (스케줄러) | 박준영 | FIFO 작업 큐, GPU 선택, Local/Docker 학습 실행 관리 |
| 3. Trainer (학습) | 김승겸 | CNN/Linear 모델 구현, 학습 수행, 가중치·지표 저장 |
| 4. Dashboard (대시보드) | 정유찬 | 작업 상태, GPU 사용률, 요청량 예측 및 학습 결과 표시 |

---

## 2. 시스템 구조

### 2.1 전체 아키텍처

본 시스템은 클라이언트 입력, FastAPI 백엔드, GPU 스케줄러, 학습 실행 레이어, 대시보드 출력의 5개 레이어로 구성된다.

```text
사용자
  |
  v
Input Web UI (1. Input)
  |
  v
FastAPI Backend (backend/)
  |
  v
FIFO Job Queue
  |
  v
GPU Scheduler (2. Scheduler)
  |
  +---- 가용 GPU 있음: 가장 여유 있는 GPU 선택
  |
  +---- GPU가 모두 사용 중: 2초 간격 폴링 대기
  |
  +---- GPU 장치 없음: CPU fallback
  |
  v
Local 또는 Docker Trainer (3. Trainer)
  |
  v
모델 가중치 및 학습 지표 저장
  |
  v
Dashboard API 및 웹 화면 (4. Dashboard)
```

| 단계 | 컴포넌트 | 역할 |
| --- | --- | --- |
| 1 | Client Input (`1. Input`) | 데이터셋 ZIP 업로드, 모델·학습 파라미터 설정 |
| 2 | FastAPI Backend (`backend/`) | 요청 수신, 작업 생성, GPU 상태 조회, 예측 API 제공 |
| 3 | GPU Scheduler (`2. Scheduler`) | FIFO 큐 기반 작업 관리, GPU 가용성 확인 및 선택 |
| 4 | Local / Docker Runner | 학습 실행 (개발 환경: LocalRunner, GPU 환경: DockerRunner) |
| 5 | Trainer (`3. Trainer`) | CNN / Linear 모델 학습, 결과 파일 저장 |
| 6 | Dashboard (`4. Dashboard`) | 실시간 작업 상태, GPU 사용률, 예측 요청 수 표시 |

### 2.2 프로젝트 디렉터리 구조

```text
Cloud-Virtualization-TeamProject/
├── 1. Input/                 # 업로드 UI (HTML/CSS/JS)
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── 2. Scheduler/             # FIFO 스케줄러, GPU 관리
│   └── scheduler/
│       ├── cli.py            # 독립 실행 CLI (local/docker 모드)
│       ├── fifo_scheduler.py # 작업 큐 관리 및 실행
│       ├── gpu.py            # nvidia-smi 기반 GPU 조회·선택
│       ├── job.py            # TrainingJob 정의
│       └── runner.py         # Local/Docker Runner 추상화
├── 3. Trainer/               # 모델 학습 코드
│   └── trainer/
│       ├── models.py         # CNN/Linear 모델 정의
│       └── train.py          # 학습 루프 및 산출물 저장
├── 4. Dashboard/             # 실시간 대시보드 (HTML/CSS/JS)
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── backend/
│   ├── main.py               # FastAPI 진입점
│   ├── job_service.py        # 작업 생성/관리, Worker Thread
│   ├── gpu_monitor.py        # nvidia-smi GPU 상태 조회
│   ├── predictor.py          # 시간대별 요청 수 예측
│   └── docker_manager.py     # Docker 실행 설정
├── scripts/
│   ├── check_docker_gpu.ps1  # GPU 환경 사전 점검 스크립트
│   └── docker_run.ps1        # Docker 실행 스크립트
├── Dockerfile                # PyTorch/CUDA 기반 컨테이너
├── docker-compose.yml
├── DOCKER.md                 # Docker/CUDA 실행 가이드 및 트러블슈팅
└── requirements.txt
```

---

## 3. 사용 기술

| 영역 | 기술 | 사용 목적 |
| --- | --- | --- |
| Frontend | HTML, CSS, JavaScript, Chart.js | 학습 요청 입력, 대시보드 및 학습 그래프 구현 |
| Backend | Python, FastAPI, Uvicorn | 파일 업로드, 작업 생성, 상태 조회 API |
| AI Training | PyTorch, torchvision | 이미지 분류 모델과 학습 루프 구현 |
| Scheduling | Python Queue, Thread | FIFO 작업 처리 및 백그라운드 실행 |
| GPU Monitoring | `nvidia-smi` | GPU 사용률과 메모리 사용량 조회 |
| Virtualization | Docker, Docker Compose | CUDA 기반 실행 환경 구성 및 데이터 볼륨 연결 |
| State Storage | JSON, CSV, PyTorch weights | 작업 상태와 학습 결과 저장 |

`requirements.txt`에 선언된 Python 패키지는 `fastapi`, `uvicorn[standard]`, `python-multipart`, `torch`, `torchvision`이다.

---

## 4. 구현 내용

### 4.1 Input (1. Input) — 김동주

사용자 인터페이스는 HTML/CSS/JavaScript로 구현되었으며, 입력값을 `FormData`(multipart/form-data)로 구성해 FastAPI 백엔드의 `POST /api/jobs`로 전송한다. 요청이 성공하면 발급받은 Job ID를 표시하고 해당 작업이 선택된 대시보드로 이동한다.

**주요 입력 파라미터**

- **Dataset ZIP**: 클래스별 폴더로 구성된 이미지 ZIP 파일
- **Model**: CNN 또는 Linear 선택
- **Epochs**: 학습 반복 횟수 (1~100)
- **Batch Size**: 미니배치 크기 (기본값 32)
- **Learning Rate**: 학습률 (기본값 0.001)
- **Image Size**: 입력 이미지 크기 (기본값 64px)

**사용성 개선 사항**

- 파일 선택 시 파일명과 용량(MB)을 즉시 표시하여 업로드 대상 확인 가능
- Dataset ZIP 폴더 구조 규칙을 입력 화면에 안내문으로 제공
- Learning Rate 입력의 step 제약을 완화(`step="any"`)하여 임의 학습률 입력 지원

**데이터셋 형식**

업로드 ZIP은 `torchvision.datasets.ImageFolder` 구조를 따르며, 클래스명으로 이름 붙여진 하위 폴더가 최소 2개 이상 필요하다.

```text
dataset.zip
├── cat/
│   ├── image1.png
│   └── image2.png
└── dog/
    ├── image1.png
    └── image2.png
```

### 4.2 Scheduler (2. Scheduler) — 박준영

FIFO(First-In First-Out) 방식의 GPU 스케줄러를 구현하였다. 작업이 제출되면 큐에 등록되고, GPU 가용 상태를 확인한 후 순서대로 실행된다.

**핵심 클래스**

| 클래스 | 역할 |
| --- | --- |
| `FifoScheduler` | 작업 큐 관리, GPU 대기 및 실행, 상태 파일 저장 |
| `GpuManager` | `nvidia-smi`를 통한 GPU 목록 조회 및 가용 GPU 선택 |
| `TrainingJob` | 작업 메타데이터 (Job ID, 모델, 하이퍼파라미터, 상태, GPU ID, 시간 정보) |
| `LocalTrainerRunner` | 개발 환경에서 Python 서브프로세스로 직접 실행 |
| `DockerTrainerRunner` | GPU 환경에서 Docker 컨테이너로 실행 |

**작업 상태 전이**

```text
queued -> running -> completed
                  -> failed
```

각 작업은 12자리 UUID 기반 Job ID로 식별된다. 백엔드의 `JobService`는 하나의 백그라운드 Worker Thread에서 큐의 작업을 순서대로 처리한다. 따라서 현재 구현은 FIFO 순서를 보장하며 동시에 하나의 작업을 실행한다. 작업 등록 시 모델명, Epoch, Batch size, Learning rate에 대한 유효성 검증(`validate()`)을 수행하며, 작업 상태가 변경될 때마다 작업별 JSON 파일과 큐 스냅샷(`queue.json`)을 디스크에 기록한다. Scheduler는 독립 CLI로도 실행할 수 있으며 `local`과 `docker` 실행 모드를 선택할 수 있다.

**GPU 선택 알고리즘**

`GpuManager`는 `nvidia-smi`를 실행해 GPU 번호, 연산 사용률, 사용 중 메모리, 전체 메모리를 조회한다. 사용률 80% 이하, 메모리 사용률 90% 이하인 GPU를 후보로 선정하고, 사용률이 낮은 순(동률 시 메모리 사용률이 낮은 순)으로 정렬하여 가장 여유 있는 장치를 자동 선택한다.

GPU 가용성에 따른 동작은 세 가지로 구분된다.

- **가용 GPU 존재**: 해당 GPU ID를 작업에 할당하고 즉시 실행한다.
- **GPU는 있으나 모두 임계값 초과**: 2초 간격으로 폴링하며 GPU가 여유로워질 때까지 대기한다.
- **GPU 장치 또는 `nvidia-smi` 없음**: CPU fallback으로 자동 전환되어, GPU가 없는 PC에서도 전체 처리 흐름을 개발하고 검증할 수 있다.

### 4.3 Trainer (3. Trainer) — 김승겸

PyTorch 기반 이미지 분류 모델을 학습하는 모듈이다. `torchvision.datasets.ImageFolder`로 데이터셋을 읽고, 전체 데이터의 20%(`--val-ratio` 옵션으로 조정 가능)를 Validation 데이터로 분리한다. 입력 이미지는 지정된 크기로 조정한 뒤 Tensor 변환과 정규화를 수행한다. 난수 seed(기본값 42)를 고정하고 동일한 generator로 데이터를 분할하므로 같은 조건에서 학습 결과를 재현할 수 있다.

**CNN 모델 구조**

```text
Conv2d(3→32)  + BatchNorm2d + ReLU + MaxPool2d
Conv2d(32→64) + BatchNorm2d + ReLU + MaxPool2d
Conv2d(64→128)+ BatchNorm2d + ReLU + AdaptiveAvgPool2d(1,1)
Linear(128 → num_classes)
```

**Linear 모델 구조**

```text
Flatten → Linear(3×size×size → 256) → ReLU → Dropout(0.2) → Linear(256 → num_classes)
```

Linear 모델은 구조가 단순해 CNN과의 학습 결과 및 실행 시간 비교에 활용할 수 있다. 두 모델 모두 Cross Entropy Loss와 Adam Optimizer를 사용하며, 각 Epoch에서 Train/Validation loss와 accuracy를 계산하고 Validation accuracy가 가장 높은 모델을 별도로 저장한다.

**학습 산출물**

| 파일 | 내용 |
| --- | --- |
| `best_model.pt` | Validation accuracy가 가장 높았던 Epoch의 가중치 |
| `final_model.pt` | 마지막 Epoch의 가중치 |
| `metrics.csv` | Epoch별 train/val loss, accuracy, 실행 시간 |
| `history.json` | 대시보드 그래프 연동용 학습 기록 (매 Epoch마다 갱신) |
| `classes.json` | 데이터셋 클래스 이름 |
| `status.json` | 실시간 진행 상태 또는 최종 결과 |

`status.json`은 학습 단계에 따라 `loading_dataset → training → completed`(실패 시 `failed`)로 갱신되며, 학습 중에는 현재 Epoch, 최신 지표, 최고 Validation accuracy를 포함한다. 학습 도중 예외가 발생하면 오류 메시지를 포함한 `failed` 상태가 기록되어 대시보드에서 실패 원인을 확인할 수 있다.

### 4.4 Dashboard (4. Dashboard) — 정유찬

실시간으로 작업 상태를 모니터링하는 대시보드이다. 2초 간격으로 백엔드 API(작업 목록, 선택 작업, GPU 상태, 요청량 예측)를 폴링하여 정보를 갱신한다.

**표시 정보**

- 등록된 전체 작업 수 및 작업별 Job ID, 모델, Epoch, 경과 시간
- 작업 상태 배지 (`queued` / `running` / `completed` / `failed`)
- GPU별 사용률 (GPU 드라이버 미설치 시 CPU fallback 안내 메시지 출력)
- 선택 작업의 설정과 Trainer 진행 상태(`status.json`) 상세
- `RequestPredictor` 기반 다음 시간대 예상 요청 수

**차트 시각화**

Chart.js를 이용해 학습 결과와 자원 상태를 그래프로 표시한다.

- **Loss 차트**: 선택 작업의 Epoch별 Train/Validation loss 라인 차트
- **Accuracy 차트**: Epoch별 Train/Validation accuracy 라인 차트
- **GPU 차트**: GPU 장치별 연산 사용률(%)과 메모리 사용률(%) 막대 차트

차트는 2초 폴링 주기에 맞춰 갱신되므로 학습이 진행되는 동안 Epoch이 끝날 때마다 그래프에 실시간으로 반영된다. 이를 위해 Trainer가 `history.json`을 학습 종료 시점이 아닌 매 Epoch마다 갱신하도록 변경하였고, 백엔드의 개별 작업 조회 API가 `trainer_history`(Epoch별 지표 배열)를 함께 응답하도록 확장하였다. 학습 기록이나 GPU가 없는 경우에는 차트 대신 안내 문구를 표시한다.

### 4.5 요청량 예측 (RequestPredictor)

요청이 등록될 때마다 현재 타임스탬프를 `logs.json`에 기록하고, 3600초 단위로 시간당 요청 수를 집계한다. 최근 최대 5개 시간대 요청 수의 이동 평균을 다음 시간대의 예상 요청 수로 사용하여, 부하 급증을 사전에 감지할 수 있는 기반을 마련하였다.

이 방식은 구현과 설명이 간단하고 데이터가 적어도 동작한다는 장점이 있다. 다만 별도의 머신러닝 모델을 학습하는 예측은 아니며, 요청이 없어 비어 있는 시간대가 평균 계산에서 제외되는 한계가 있다.

---

## 5. 백엔드 API 명세 (FastAPI)

| Method | Endpoint | 기능 |
| --- | --- | --- |
| GET | `/` | Input `index.html` 직접 응답 |
| GET | `/api/health` | 서버 상태 확인 |
| POST | `/api/jobs` | 학습 작업 제출 (dataset ZIP, model, epochs 등) |
| GET | `/api/jobs` | 전체 작업 목록 조회 |
| GET | `/api/jobs/{job_id}` | 개별 작업 상세 조회 (Trainer 상태 및 Epoch별 학습 이력 포함) |
| GET | `/api/gpu` | 현재 GPU 사용률·메모리 상태 조회 |
| GET | `/api/predict` | 다음 시간대 요청 수 예측 |

이외에 `/input/`과 `/dashboard/` 경로로 각 웹 화면이 정적 파일로 제공된다.

업로드된 ZIP은 작업별 디렉터리(`data/uploads/{job_id}`)에 저장되고 압축 해제된다. 데이터셋 처리 시 다음 검증을 수행한 후 작업을 큐에 등록한다.

- `.zip` 확장자만 허용
- 압축 파일의 상위 경로 이동(path traversal) 차단
- 이미지(jpg, jpeg, png, bmp, gif, webp)가 존재하는 클래스 폴더 2개 이상 확인
- 클래스 폴더들이 ZIP 루트가 아닌 단일 상위 폴더로 감싸진 경우도 자동 인식

또한 메모리에 없는 작업을 조회하면 디스크의 상태 파일(`{job_id}.json`)을 읽어 응답하므로, 서버 재시작 후에도 과거 작업의 기록을 확인할 수 있다.

---

## 6. Docker 및 가상화 구성

Docker 이미지는 CUDA 12.4 및 cuDNN 9 Runtime이 포함된 PyTorch 공식 이미지(`pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime`)를 기반으로 한다. 애플리케이션 코드와 의존성을 이미지에 복사하고, `PYTHONPATH`에 Scheduler와 Trainer 디렉터리를 등록한 뒤 Uvicorn으로 FastAPI 서버를 실행한다.

Docker Compose는 다음 호스트 디렉터리를 컨테이너와 공유하여 컨테이너 재시작 후에도 데이터가 보존되도록 구성하였다.

| Host | Container | 목적 |
| --- | --- | --- |
| `./data` | `/app/data` | 업로드 데이터 보존 |
| `./3. Trainer/outputs` | `/app/3. Trainer/outputs` | 학습 결과 보존 |
| `./2. Scheduler/scheduler_state` | `/app/2. Scheduler/scheduler_state` | 작업 상태 보존 |

Compose 환경에서는 호스트의 NVIDIA GPU 전체를 컨테이너에 연결하도록 설정되어 있으며(`gpus: all`), 서버는 컨테이너의 8000번 포트를 호스트의 8010번 포트로 공개한다.

현재 FastAPI 통합 서버의 기본 Runner는 `local`이다. 따라서 Docker Compose로 전체 애플리케이션을 실행할 경우 Trainer는 별도 하위 컨테이너가 아니라 FastAPI가 실행 중인 CUDA 컨테이너 내부에서 subprocess로 실행된다. Scheduler의 `docker` 모드는 CLI 수준에서 별도 학습 컨테이너를 실행할 수 있도록 구현되어 있다.

`DockerTrainerRunner`는 작업마다 `cv-trainer-{job_id}` 이름의 일회용 컨테이너(`--rm`)를 생성하고, 선택된 GPU를 `--gpus device={gpu_id}` 옵션으로 해당 컨테이너에만 노출한다. 데이터셋과 Trainer 코드는 읽기 전용(read-only)으로, 결과 디렉터리는 읽기/쓰기로 마운트하여 컨테이너가 호스트 데이터를 변경할 수 없도록 격리하였다. Docker 컨테이너 제어는 Python Docker SDK가 아니라 `subprocess`에서 Docker CLI의 `docker run` 명령을 호출하는 방식으로 구현하였다.

---

## 7. 실행 환경 및 실행 방법

### 7.1 호스트 요구사항

| 구분 | 요구사항 |
| --- | --- |
| OS | Windows / Linux |
| Python | 3.10 이상 |
| GPU 실행 시 | NVIDIA GPU + CUDA 호환 드라이버 + NVIDIA Container Toolkit |
| Docker | Docker Desktop 또는 Docker Engine (Windows는 WSL2 기반 Linux engine 필요) |
| 주요 패키지 | fastapi, uvicorn[standard], python-multipart, torch, torchvision |

Docker/CUDA 환경 구성 절차와 문제 해결 방법은 저장소의 `DOCKER.md`에 별도로 문서화하였다.

### 7.2 실행 방법

**개발 환경 (Local 모드)**

```bash
pip install -r requirements.txt
uvicorn backend.main:app --reload
# 접속: http://127.0.0.1:8000/input/
```

**Docker + GPU 환경**

```powershell
.\scripts\check_docker_gpu.ps1   # GPU 환경 사전 점검 (컨테이너 내부 nvidia-smi 확인)
docker compose up --build
# Input:     http://127.0.0.1:8010/input/
# Dashboard: http://127.0.0.1:8010/dashboard/
```

### 7.3 전체 실행 흐름

1. 사용자가 Input 화면에서 ZIP 데이터셋과 학습 옵션을 제출한다.
2. FastAPI가 요청 시간을 기록하고 파일을 서버에 저장한다.
3. ZIP을 압축 해제하고 클래스 폴더와 이미지 존재 여부를 검사한다.
4. Job ID를 생성하고 작업 상태를 `queued`로 저장한다.
5. 백그라운드 Worker가 가장 먼저 등록된 작업을 꺼낸다.
6. `nvidia-smi`로 사용 가능한 GPU를 조회한다.
7. 가용 GPU가 있으면 GPU ID를 지정하고, 모두 사용 중이면 대기하며, GPU 장치가 없으면 CPU를 사용한다.
8. Trainer를 별도 Python 프로세스 또는 Docker 컨테이너로 실행한다.
9. Trainer가 Epoch별 학습 지표와 상태 파일을 저장한다.
10. 작업 성공 시 `completed`, 오류 발생 시 `failed` 상태로 변경한다.
11. Dashboard가 API를 주기적으로 호출하여 최신 상태를 표시한다.

---

## 8. 구현 및 검증 결과

### 8.1 확인된 항목

- Python 소스 15개 파일의 문법 분석 통과
- 가짜 Trainer를 이용한 Scheduler 독립 시험에서 3개 작업의 FIFO 실행 순서 확인
- CPU fallback 환경에서 모든 시험 작업이 `completed`로 변경되는 것 확인
- 작업별 JSON과 `queue.json` 상태 파일 생성 확인
- 요청 3건 기록 후 다음 시간대 예상 요청 수가 3.0으로 계산되는 것 확인
- Docker Compose 설정 구문 검증 통과
- 반응형 Input 및 Dashboard 웹 화면 구현 확인
- 동일한 API 응답 형태의 목(mock) 서버를 이용한 브라우저 시험에서 Loss/Accuracy 라인 차트, GPU 막대 차트의 렌더링·주기 갱신·데이터 없음 안내 표시 확인

### 8.2 검증 환경 및 제약

검증은 macOS(Python 3.14.3, Docker 29.2.1, Docker Compose 5.1.0) 환경에서 수행하였다. 해당 로컬 환경에는 FastAPI, PyTorch 및 torchvision이 설치되어 있지 않아 실제 이미지 학습과 FastAPI End-to-End 실행은 수행하지 않았으며, NVIDIA GPU와 `nvidia-smi`가 없어 CUDA GPU 할당은 코드 및 Docker 설정 기준으로 분석하였다.

프로젝트에서 의도한 실행 환경은 Python 3.10 이상이 설치된 Windows 또는 Linux 호스트이며, GPU 실행에는 NVIDIA GPU, CUDA 호환 드라이버 및 NVIDIA Container Toolkit이 필요하다.

---

## 9. 기획 대비 구현 결과

| 기획 내용 | 실제 구현 | 평가 |
| --- | --- | --- |
| 웹에서 데이터셋과 모델 옵션 입력 | HTML/JS Input 화면 및 FastAPI 연동 | 구현 완료 |
| FIFO 작업 큐 | Python Queue와 단일 Worker Thread | 구현 완료 |
| CNN/Linear 학습 | PyTorch 모델, 학습 루프, 결과 저장 | 구현 완료 |
| GPU 상태 확인 | `nvidia-smi` 사용률 및 메모리 조회 | 구현 완료 |
| Docker SDK로 컨테이너 제어 | `subprocess`를 통한 Docker CLI 호출 | 대체 방식으로 구현 |
| GPU 자원 비율 동적 할당 | 임계값 조건을 만족하는 GPU 장치 하나를 선택 | 부분 구현 |
| 과거 요청으로 AI 수요 예측 | 최근 시간대 요청 수의 이동 평균 | 단순화 구현 |
| 결과 시각화 | Chart.js 기반 Loss/Accuracy 라인 차트, GPU 막대 차트 | 구현 완료 |
| 클라우드 배포 | 로컬 및 Docker 호스트 실행 구성 | 실제 클라우드 배포 미수행 |

---

## 10. 프로젝트의 장점

1. Input, Scheduler, Trainer, Dashboard의 책임이 분리되어 있어 구조를 이해하고 기능을 확장하기 쉽다.
2. 웹 요청부터 모델 학습 결과 저장까지 전체 처리 흐름이 하나의 파이프라인으로 연결되어 있다.
3. GPU가 없는 환경에서도 CPU fallback으로 개발과 기능 확인이 가능하다.
4. 작업과 학습 상태가 JSON 및 CSV로 저장되어 장애 분석과 결과 재사용이 가능하다.
5. Local Runner와 Docker Runner를 추상화해 실행 환경을 교체할 수 있다.
6. 데이터셋 검증, 하이퍼파라미터 검증 및 작업 실패 상태 처리가 포함되어 있다.

---

## 11. 한계 및 개선 방향

### 11.1 실제 GPU 자원 분할 미구현

현재 Scheduler는 GPU 장치 번호를 선택하지만 GPU 연산 능력이나 메모리를 40%, 60%와 같이 비율로 분할하지는 않는다. NVIDIA MPS, MIG 또는 Kubernetes GPU Device Plugin을 활용하면 보다 실제적인 자원 가상화 구조로 확장할 수 있다.

### 11.2 동시 작업 처리 제한

백엔드는 Worker Thread 하나로 작업을 처리하므로 여러 GPU가 있어도 한 번에 하나의 학습만 실행한다. GPU별 Worker Pool과 GPU 예약 상태를 도입하면 여러 작업을 병렬 처리할 수 있다.

### 11.3 프로세스 재시작 복구 부족

개별 작업 상태는 디스크에 저장되고 메모리에 없는 작업도 상태 파일을 읽어 단건 조회는 가능하지만, 서버 시작 시 기존 작업을 메모리(작업 목록·큐)로 복원하지는 않는다. 재시작 시 `queued` 또는 `running` 작업을 큐에 복구하는 로직이 필요하다.

### 11.4 요청량 예측 고도화 필요

현재 예측은 최근 요청 수의 단순 이동 평균이다. 시간대, 요일, 추세를 반영하는 회귀 모델이나 시계열 모델을 적용하고 예측 결과를 실제 GPU 사전 할당과 연결할 수 있다.

### 11.5 대시보드 시각화 고도화

Epoch별 loss/accuracy 라인 차트와 GPU 사용률 막대 차트는 구현되었으나, GPU 사용률의 시간대별 추이 그래프, 여러 작업 간 학습 곡선 비교, 차트 이미지 내보내기 등은 제공하지 않는다. 또한 차트 라이브러리를 CDN으로 로드하므로 오프라인 환경에서는 그래프 대신 안내 문구만 표시된다.

### 11.6 운영 안정성과 보안

업로드 용량 제한, 사용자 인증, 작업 취소, 로그 동시 접근 제어, 오래된 파일 정리 및 보다 엄격한 ZIP 경로 검증이 필요하다. 운영 환경에서는 작업 정보를 SQLite 또는 PostgreSQL에 저장하고 Redis/Celery 같은 별도 작업 큐를 사용하는 것이 적합하다.

### 11.7 자동화된 테스트 부족

현재 저장소에는 단위 테스트와 통합 테스트가 포함되어 있지 않다. Scheduler 순서, 데이터셋 검증, API 응답, Trainer 산출물 및 실패 복구를 자동으로 확인하는 테스트를 추가할 필요가 있다.

---

## 12. 결론

### 12.1 구현 결과 요약

본 프로젝트는 사용자가 웹에서 데이터셋과 학습 옵션을 제출하면 FastAPI가 요청을 접수하고, FIFO Scheduler가 GPU 상태를 확인한 뒤 PyTorch Trainer를 실행하며, Dashboard가 처리 상태와 결과를 제공하는 전체 구조를 구현하였다.

- FastAPI 기반 RESTful API 서버 구현
- FIFO 작업 큐와 `GpuManager`를 통한 가용 GPU 자동 선택 구현
- CNN / Linear 두 가지 모델 아키텍처 지원 및 학습 결과 파일 저장
- 실시간 대시보드를 통한 학습 상태, GPU 사용률, 요청량 예측 표시 및 Loss/Accuracy·GPU 차트 시각화
- Local 및 Docker 두 가지 실행 모드 지원으로 환경 유연성 확보

특히 입력, 스케줄링, 학습 및 출력 영역을 독립된 모듈로 분리하고 Local/Docker 실행 방식을 함께 제공했다는 점에서 클라우드 기반 AI 학습 서비스의 기본 구조를 실습했다는 의미가 있다. 모델 가중치와 학습 지표가 실제 파일로 저장되고 작업 상태가 API로 제공되므로 단순 화면 시연을 넘어 학습 파이프라인의 주요 구성 요소가 연결되어 있다.

### 12.2 개발 환경 제약 및 평가

개발 과정에서는 단국대학교 클라우드 인프라에서 사용할 수 있는 GPU 자원이 확인되지 않아 팀원 개인 PC를 호스트로 활용하였다. 실제 클라우드 환경이 아닌 단일 로컬 머신에서 동작한다는 점에서 본래 목표로 했던 클라우드 가상화의 취지와 완전히 부합하지는 않는다는 한계가 있다.

또한 학습에 사용한 CNN, Linear 모델은 시스템의 동작 흐름을 검증하기 위한 기초 모델로, GCP나 AWS에서 실제로 서비스되는 대규모 모델(LLM, ResNet-50 등)과는 규모 면에서 차이가 있다. 그럼에도 자원 요청부터 스케줄링, 컨테이너 실행, 결과 모니터링까지의 전체 파이프라인을 직접 설계하고 구현하였다는 점에서 클라우드 가상화 기술의 핵심 흐름을 경험할 수 있었다.

현재 구현은 GPU 비율 분할이나 다중 작업 병렬 처리보다 단일 호스트에서의 GPU 장치 선택과 순차 처리에 초점을 둔 프로토타입이다. 향후 GPU Worker Pool, 지속성 데이터베이스, 시계열 수요 예측, 학습 그래프 시각화 및 Kubernetes 기반 배포를 추가하면 실제 클라우드 환경에 가까운 시스템으로 발전시킬 수 있다.

---

## 부록 A. 발표 시 강조할 핵심 내용

1. 사용자는 복잡한 PyTorch 및 CUDA 명령 없이 웹에서 데이터와 옵션만 제출한다.
2. 모든 요청은 Job ID로 관리되며 FIFO 순서로 실행된다.
3. Scheduler는 GPU 사용률과 메모리를 확인해 가장 여유 있는 GPU를 선택한다.
4. GPU가 없어도 CPU fallback으로 전체 파이프라인을 실행할 수 있다.
5. CNN과 Linear 모델을 선택할 수 있고 최적 및 최종 가중치를 모두 저장한다.
6. Dashboard는 작업, GPU 및 요청량 예측 상태를 2초 간격으로 갱신하며, Epoch별 Loss/Accuracy와 GPU 사용률을 차트로 시각화한다.
7. Docker Compose를 통해 CUDA 기반 실행 환경과 결과 저장 볼륨을 구성했다.

---

*단국대학교 | 클라우드가상화기술 1분반 | 2026년 6월 15일*
